# Docstring Issues Found During Build

This document lists the docstring issues found during the Sphinx documentation build.

## Critical Issues (Import Errors)

None - all modules import successfully.

## Warning Categories

### 1. Duplicate Object Descriptions (22 warnings)

These occur because modules are documented both in `__init__.py` and in their own files.
**Solution**: Add `:no-index:` directive to one of the locations in the RST files.

Affected modules:
- `scptensor.dim_reduction.pca`
- `scptensor.dim_reduction.umap`
- `scptensor.impute.knn`
- `scptensor.impute.missforest`
- `scptensor.impute.ppca`
- `scptensor.impute.svd`
- `scptensor.integration.combat`
- `scptensor.integration.harmony`
- `scptensor.integration.mnn`
- `scptensor.integration.nonlinear`
- `scptensor.integration.scanorama`
- `scptensor.qc.basic`
- `scptensor.qc.outlier`
- Others...

### 2. Docstring Formatting Issues (40+ warnings)

These are formatting issues in docstrings that need to be fixed:

#### Definition List / Indentation Issues

**File**: `scptensor/core/structures.py`
- `ScpContainer.__init__`: Line 4 - Definition list needs blank line
- `Assay.__init__`: Line 4 - Definition list needs blank line

**File**: `scptensor/dim_reduction/pca.py`
- `pca` function: Line 13 - Block quote ends without blank line

**File**: `scptensor/impute/knn.py`
- `knn` function: Line 10 - Block quote ends without blank line

**File**: `scptensor/impute/missforest.py`
- `missforest` function: Line 7 - Block quote ends without blank line

And similar issues in other modules.

**Solution**: Add blank lines between parameter descriptions and example sections in docstrings.

Example fix:
```python
# Before (causes warning):
def foo(x, y):
    """
    Function description.

    Parameters
    ----------
    x : int
        First param
    y : int
        Second param
    Examples
    --------
    >>> foo(1, 2)
    """

# After (fixed):
def foo(x, y):
    """
    Function description.

    Parameters
    ----------
    x : int
        First param
    y : int
        Second param

    Examples
    --------
    >>> foo(1, 2)
    """
```

## Design Doc Warnings (Can be ignored)

Warnings in `docs/design/` are from markdown files and don't affect API documentation quality.
These can be ignored or fixed later if desired.

## Summary Statistics

- **Total Warnings**: 117
- **Design Doc Warnings**: ~60 (can be ignored)
- **Real Code Warnings**: ~57
  - Duplicate descriptions: ~22
  - Formatting issues: ~35

## Recommendations

1. **High Priority**: Fix docstring formatting issues for better documentation rendering
2. **Medium Priority**: Add `:no-index:` to eliminate duplicate warnings
3. **Low Priority**: Clean up design doc warnings (cosmetic)

## Build Status

✅ **Build Successful**: Documentation generates successfully
✅ **Coverage**: All modules documented
✅ **Theme**: Furo theme applied correctly
✅ **Navigation**: Working table of contents and search

## How to View

```bash
# Serve documentation locally
cd docs/_build/html
python3 -m http.server 8000
# Visit http://localhost:8000
```
